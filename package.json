{
  "name": "houstonopolis-webflow-webhook",
  "version": "1.0.0",
  "description": "Agentic webhook: Webflow Form → Vercel API → Webflow CMS → Auto Publish",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": "24.x"
  }
}
